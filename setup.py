from setuptools import setup, find_packages

NAME = 'otfregrid'
VERSION = '0.1.dev'

setup(
    name=NAME,
    version=VERSION,
    description='an In-Progress telescope unspecific On-The-Fly regridding program',
    author='Theodore Kareta, Gopal Narayanan',
    packages=find_packages(),
    )
